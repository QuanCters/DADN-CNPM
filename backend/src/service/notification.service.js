"use strict";

class NotificationService {
  static createNotification(notification) {}
  static sendNotification(notification_id, list_user) {}
  static getNotificationByDevice(device_id) {}
}

module.exports = NotificationService;
